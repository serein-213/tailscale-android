<?xml version="1.0" encoding="utf-8"?>
<!-- Paths exposed through the ${applicationId}.fileprovider FileProvider.
     UpdateChecker downloads release APKs into the app's external files directory
     (Download/) and hands them to the package installer from there. -->
<paths>
    <external-files-path name="downloads" path="Download/" />
</paths>
